import { render } from '@testing-library/react';
import { describe, vitest } from 'vitest';

const renderLogin = () => {
    render(
        <LoginPage/>
    )
}


describe('login page should display login', () => {

})